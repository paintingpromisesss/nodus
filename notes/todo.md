# Now
1) Перевести `sender` на использование локального файла Telegram Bot API вместо пересылки через multipart: на больших файлах отправка занимает слишком много времени (HIGH PRIORITY)
2) Реализовать middleware с лимитами для каждого пользователя, чтобы исключить недобросовестное использование (HIGH PRIORITY)
3) Изучить документацию и реализовать работу бота в группах и личных сообщениях через `@ + ссылка`. Для инсты, шортсов, музыки и прочего короткого контента выдавать максимальное качество; для обычного видео на YouTube отдельно продумать поведение (HIGH PRIORITY)

# Next
1) Решить, нужны ли дефолтные настройки из БД, и если да, то как их использовать. Возможный вариант: отдельная кнопка среди tab по типу "Скачать по дефолту" (MID PRIORITY)
2) Продумать режим работы бота "по приглашениям" через параметр в env: бот должен работать только для заданного пула людей, а всех остальных игнорировать (MID PRIORITY)
3) Рассмотреть возможность добавления сервиса Spotify как поддерживаемого для скачивания, путём парсинга метаданных по ссылке и поиска соответствующего трека на Youtube Music (MID PRIORITY)

# Later
1) Решить проблему вложенной папки `tmp` (LOW PRIORITY)
2) Улучшить логирование и хэндлинг ошибок, чтобы можно было показывать пользователю понятный текст (LOW PRIORITY)
3) Продумать добавление субтитров из полученных метаданных в видео по выбору пользователя: и с точки зрения интерфейса, и с точки зрения реализации (LOW PRIORITY)
4) Допилить развёртывание бота и volume; возможно, сделать install-скрипт, который позволит всё настроить из терминала (LOW PRIORITY)
5) Реализовать progress bar в сообщении, который показывает статус скачивания медиа и статус его отправки (LOW PRIORITY)
6) Улучшить UI и информативность сообщений от бота, дочистить все спорные места (LOW PRIORITY)

# Icebox
1) Изучить возможность реализовать определение всей музыки в видео по любой ссылке: с таймкодами, названиями, возможно ссылками на прямое скачивание или кнопкой, открывающей picker-меню для скачивания музыки (предположительно с YouTube). Сырая и расплывчатая идея (LOW PRIORITY)
2) Продумать скачивание исходников без принудительного remux и merge как дополнительную фичу с отдельной кнопкой-флагом (LOW PRIORITY)

work work work
